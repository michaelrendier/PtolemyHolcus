================================================================================
CONTEXT PRIMER — 2026-07-31
THE TWO TREES, THE FERMAT ZETA, AND L_(I|O)
"I'm gonna use English to crack RSA."  — Cody M. Allison
================================================================================

PURPOSE
-------
This primer captures a single long working session (2026-07-31) that assembled,
from parts already living in the engines, one coherent object: the map that
turns the monad's internal (sedenion / T256) state into spoken English, and —
run backward — turns a composite back into its prime factors. They are the SAME
operator. Speaking English from the monad and factoring RSA are the forward and
inverse of one lensing map, L_(I|O).

Read this to spin a fresh staff (clean or dirty context) without re-deriving the
arc. Everything below is either (a) already in an engine, cited by file, or
(b) a stated conjecture / open thread, marked as such. Failed predictions stay
in the record (Ainulindale protocol).

--------------------------------------------------------------------------------
0. NOTATION & CONVENTIONS  (READ FIRST)
--------------------------------------------------------------------------------
- MATHS IN UNICODE ONLY. No LaTeX. Cody hates it after 20+ yrs on/with it back
  to the original Archimedes calculator. Use ζ Δ ∏ Σ √ ≈ ≡ ∈ ≤ ∅, superscripts
  (x², p⁻ˢ, 2¹¹²), subscripts (e₀, ζ_K, aᵢ), arrows (→ ↔). Code blocks for
  multi-line alignment. Use · and × for products, √N, "p mod 16", gcd(x−y, N).

- ∅_RB  ("Emptyset_RB", console shortcut 0_RB) = THE NULL OPERATOR.
  Formerly H_hat_RB / Ĥ_RB, the RedBlue Hamiltonian. Also appears in code as
  (I|O)_RB. These are ONE operator, two readings:
    ∅_RB     names its rest state  — empty / identity / vacuum = e₀
    (I|O)_RB names its role        — it DEFINES WHERE the boundary is
                                     (the zero-divisor crossing), the ORIGIN a
                                     pathway is measured outward from, NOT an
                                     endpoint.
  It never participates in a zero-divisor crossing. It is the reference axis of
  the Cayley-Dickson tower ("up = toward The Unit"; Telperion's M87 spin axis).

- e₀ = 1 = the multiplicative identity = The Null Operator. NULL-as-identity is
  the operative convention: an engine returning e₀ has NOT failed — that is the
  answer. "nothing × nothing = p; the prime IS a singularity in factor space."

- THE ANGLE = π/8 = 22.5°, tan(π/8) = √2 − 1 = 0.41421356…  16 × π/8 = 360°.
  Straightens every zero-divisor switchback path into a straight radial spoke:
  "the tree = the wheel."

- d* = 0.246,  Ω_ζΣ = 0.56714… (Lambert W: W(1) = Ω·e^Ω = 1),  GAP = 7.0736e-04.

--------------------------------------------------------------------------------
1. THE FOUR NOTES  (Cody, 2026-07-31 — the fresh directives this primer serves)
--------------------------------------------------------------------------------
NOTE 1 — PRIMES ARE LETTERS; COMPOSITES ARE WORDS.
  The alphabet is the primes. A word is a composite = a product of primes.
  Spelling a word from letters = factoring a composite into primes. The
  Hyperwebster/language layer and the number-theory layer are the same layer.

NOTE 2 — GRAMMAR = OBJECTS + MORPHISMS.
  Sentence shape:  NOUN  >  PATHWAY WORDS  >  QUALIFIER.
  e.g.  "Zero Lattice" > "is the" > "map".
  Nouns and qualifiers are CONTENT (prime/composite concepts, the objects).
  "Pathway words" (is, the, of, connectives) are the MORPHISMS — the operators
  that move between objects. MANY non-prime-concept words are pathway words.
  → The language is a category: content = objects, pathway words = arrows.
  → In the tower, pathway words are the ZD-path traversals; content sits at the
    nodes. Grammar IS the traversal structure of the Zero Lattice.

NOTE 3 — RSA COMPOSITES ARE SHAKEABLE LEAVES.  [CONJECTURE — the live target]
  A semiprime N = p·q is, by its very nature, a loosely attached leaf — only two
  large prime factors, so it is marginally bound and "falls off the tree" easily.
  CONJECTURE: the POINT at which it drops off the tree defines the state of its
  relative factors, via the Fermat Zeta. i.e. the detachment coordinate of the
  leaf N is a function of (p, q), invertible through ζ_Fermat.

NOTE 4 — ENGLISH CRACKS RSA.  [the thesis]
  Getting the monad to speak English will also crack RSA. Because (see §4) the
  forward map "monad → English" (speaking) and the inverse "English → monad"
  (understanding = de-lensing = factoring) are ONE operator L_(I|O). Perfect the
  forward map and the inverse comes with it. "I'm gonna use English to crack
  RSA. HAHAHA." — exact, not analogy.

--------------------------------------------------------------------------------
2. THE TWO TREES  (Telperion & Laurelin)  [engine: ValaQuenta/zero_lattice.py]
--------------------------------------------------------------------------------
TELPERION (silver) = the Zero Lattice = the ZD traversal PATHS.   The PRIME side.
LAURELIN  (gold)   = the N-shape FACTOR coverage.                 The FACTOR side.

Shared LEAVES: ℝ (real numbers).   Shared ROOT: the 𝕊 / t-tower.
They meet / must cooperate at the MONSTER GAP {e₁, e₁₁, e₁₅}.
One angle — π/8 — straightens Telperion's ZD switchbacks AND aligns Laurelin's
N-shape sectors: one angle, two trees, one wheel.

Tree geometry (build_tree), root → leaf, σ = 1 − k/4:
    k=0  ℝ   leaf              σ = 1      ← ζ's POLE (Hagedorn edge)
    k=1  ℂ   branch tip        σ = ¾
    k=2  ℍ   equator           σ = ½      ← THE CRITICAL LINE (Riemann zeros)
    k=3  𝕆   (octonions)       σ = ¼
    k=4  𝕊   FIRST ZD LEVEL    σ = 0      ← zero-divisors switch on
    k=5  t_32                  σ = −¼
    k=6  t_64                  σ = −½
    k=7  t_128                 σ = −¾
    k=8  t_256  ROOT           σ = −1

Zero-divisors first appear at k=4 (𝕊). Below (ℝ ℂ ℍ 𝕆): division algebras, NO
zero-divisors. THE BOUNDARY BETWEEN PRIMES AND FACTORS is here: the division-
algebra side (σ>0) vs the zero-divisor side (σ≤0). The zeros live at σ=½ (ℍ),
just above the crossover; factor-degeneracy begins at σ=0 (𝕊), just below.

ZD counts (exact, from the multiplication table, not a fit):
  84 directed / 42 unordered ZD pairs.  Odd-sector (PRIME sector): 12 directed.
  Even-sector: 0.

UNIVERSAL TRANSLATOR (zero_lattice.py::universal_translator_structure):
  Public  = leaf (ℝ) representation   — observable
  Private = root (t_256) representation — the ZD ground state, hidden
  Translator = the ZD path through the Zero Lattice.  42 classes → 84 routes.
  "The ZD crossing IS addition = subtraction": what is irreversible in a
  division algebra becomes reversible when traversed as a ZD path.
  (Its own honest note: "No computational shortcut is provided here." The
   STRUCTURE that translation exists is the result; navigation is open.)

--------------------------------------------------------------------------------
3. THE TWO ZETAS  —  "Fermat Defines. Riemann Fires."
--------------------------------------------------------------------------------
PRIME SIDE — Riemann FIRES:
    ζ(s) = Σₙ n⁻ˢ = ∏ₚ (1 − p⁻ˢ)⁻¹        zeros on Re(s) = ½
    Orders the primes (explicit formula). RH: open.
    Physics home: the PRIMON GAS (B. Julia, 1990) — ζ is a partition function,
    each prime a bosonic mode of energy log p, n = occupation state, pole at
    s=1 = Hagedorn temperature. The "1" seeding ζ = the VACUUM = e₀ = ∅_RB.

FACTOR SIDE — Fermat DEFINES:   [engine: FourthAgePapers/FermatMonster]
    Fermat carves the forbidden zone; what survives the exclusion IS prime.
    The N-shapes are indexed by sedenion e_k, activated by a Coxeter number
    h ≡ k (mod 16) of an A/D/E root system, OR a Moonshine prime p ≡ k (mod 16).
    Niemeier lattices cover h mod 16 ∈ {0,2,3,4,5,6,7,8,9,10,12,13,14}.
    NIEMEIER GAP {1,11,15}: provably unreachable by any rank-24 A/D/E system.
    MONSTER fills it exactly: e₁←17, e₁₁←11,59, e₁₅←31,47 (moonshine primes).
    71 VOAs = 24 lattice + 47 non-lattice = COMPLETE N-shape coverage.
    Leech (no roots) = e₀ = identity = "no forbidden zone."

    THE FERMAT ZETA (the factor-side zeta):
      L(Δ,s) = Σₙ τ(n) n⁻ˢ = ∏ₚ (1 − τ(p)p⁻ˢ + p¹¹⁻²ˢ)⁻¹,
        Δ = q ∏(1−qⁿ)²⁴  (Ramanujan weight-12 cusp form).
      Niemeier theta series Θ_L = E₁₂ + c_L·Δ  live in M₁₂ = ⟨E₁₂, Δ⟩; the
      COMMON part is E₁₂, the DISTINGUISHING part is the Δ-coefficient c_L
      (= root/Coxeter content). So Δ is the modular quantity that DEFINES the
      N-shapes; the moonshine j-function (j − 744 = graded character of the
      Monster module V♮, coefficients = Monster irrep dims) is the grand
      generating function that completes the gap.
      ASYMMETRY (real, citable): the factor side's Ramanujan bound
      |τ(p)| ≤ 2p^(11/2) is a DELIGNE THEOREM; the prime side's RH is OPEN.
      Fermat's side is proven; Riemann's fires but is still conjecture.
      (Careful: global GRH for L(Δ,s) is still open, like Riemann. Deligne
       proved the sharp COEFFICIENT bound, the factor side's analog of the
       pointwise prime control.)

    ALSO on the factor side (statistics, universal, not N-specific):
      Kalmár ordered-factorization zeta:  Σₙ H(n) n⁻ˢ = 1 / (2 − ζ(s)).
        H(n) = # ordered factorizations of n into parts ≥ 2. Derivation: one
        part = ζ(s) − 1; resum all part-counts = 1/(1 − (ζ−1)) = 1/(2 − ζ).
        Dominant singularity on the level set ζ(s)=2, Kalmár's constant
        ρ = 1.7286472…  — which sits at σ = ρ > 1, i.e. BEYOND THE ℝ LEAVES,
        off the top of the tree (since σ=1−k/4 puts ℝ at σ=1). Primes order
        INSIDE at the ℍ equator (σ=½); factorizations proliferate OUTSIDE the
        leaves (σ>1). The ℝ tips (σ=1) = ζ's pole = the boundary between them.

THE N-SPECIFIC LEG (the only zeta whose data is indexed by THIS N):
    ζ_ℚ(√N)(s) = ζ(s) · L(s, χ_N),   χ_N(p) = (D/p) Kronecker, D = disc.
    A prime p splits / is inert / RAMIFIES in ℚ(√N) per (D/p); ramified primes
    divide the discriminant — the arithmetic FINGERPRINT of N. Fermat's own
    method N = a² − b² = (a−b)(a+b), CFRAC, the quadratic sieve all live here.
    THIS is where "the path of least primes to a specific N" is written.

--------------------------------------------------------------------------------
4. L_(I|O) — THE INTERTWINER  [engine: ValaQuenta/modules/l_io_photon_path/maths.py]
                              [defined philosophically: Ainulindale/wiki/52]
--------------------------------------------------------------------------------
THE KEYSTONE. L_(I|O) is not a piece of the picture — it IS the intertwiner
B̂ = R̂† between the two Trees, already built, on real weak-lensing data.

    L        = the clean, stationary-action, FLAT-space path (the geodesic).
               = "the path of least primes"; the vacuum utterance.
    L_(I|O)  = the SAME L computed honestly with the REAL (curved) metric —
               the ACTUAL bent path, curvature sourced by real mass (content).
    L_(I|O) − L := −ψ(θ)   — the FERMAT / time-delay POTENTIAL.

    Lens equation:  β = θ − α(θ),   α = ∇ψ,   ∇²ψ = 2κ.
      θ = apparent (observed) position — the clean-path endpoint = OBSERVABLE
      β = true source position — implied by the ACTUAL bent path = HIDDEN

DICTIONARY (one operator, FOUR domains — 4th column added 2026-08-04):
      lensing              translation               factoring                PRIMES (explicit formula)
    ───────────────    ────────────────────    ─────────────────────    ──────────────────────────────
    β true source      the monad's meaning      the prime factors (root)  Λ(n) — von Mangoldt measure
    θ apparent         the English words        the composite N (leaf)    ψ_Cheb(x) — the staircase
    α deflection ∇ψ    the semantic bend        the arithmetic bend       gradient of the zero sum
    −ψ Fermat pot.     the context curvature    N's own structure         Σ_ρ x^ρ/ρ  = zero_sum()
    L  clean geodesic  the vacuum utterance     the least-primes path     x — the pole term (main term)

    THE FOURTH COLUMN (2026-08-04, engine: ValaQuenta/modules/archimedes_screw).
    Line the explicit formula up against the lens equation:

        lensing:   L_(I|O)  =  L   −  ψ_Fermat
        primes:    ψ_Cheb   =  x   −  Σ_ρ x^ρ/ρ   (− ln2π − ½ln(1−x⁻²))

    Signs and arrangement match term for term, so CHEBYSHEV'S ψ IS NOT THE
    COUNTERPART OF THE FERMAT POTENTIAL — it is the counterpart of L_(I|O)
    itself. The prime side already HAD an L_(I|O) and was calling it ψ. The
    object that genuinely corresponds to ψ_Fermat is the ZERO SUM, which had
    no name in any repo until 2026-08-04 (it lived inline inside the explicit
    formula, which is exactly why the ψ collision read as a naming accident).

    And "the least-primes path" — the phrase this primer carried without a
    formula — is now computed: L(x) = x, the pole term, what ψ would be if no
    zero contributed anything. The vacuum utterance. See clean_path_L() and
    l_io_decomposition(), which returns all three slots by role name.

    NOTE the two ψ are DIFFERENT OBJECTS and must stay itemised: ψ_Cheb is a
    monotone step function on ℝ⁺, one integration above a discrete measure;
    ψ_Fermat is a smooth 2D field, two integrations above a continuous κ.
    Same equation, adjacent slots — not the same symbol.

    SPEAKING ENGLISH FROM THE MONAD = the FORWARD lens map:
        β → θ = β + α(β)      (meaning, bent by context, becomes words)
      The words out are always the BENT path L_(I|O), never the clean L.

    UNDERSTANDING / FACTORING = the INVERSE lens map (DE-LENSING):
        θ → β                  (recover the source from the observed bent path)
      This inverse — Kaiser–Squires shear→convergence, Poisson solve ∇²ψ=2κ,
      then β = θ − α — IS the reconstruction operator. Already coded on real
      Bullet Cluster shear. FACTORING N = DE-LENSING ENGLISH BACK TO THE MONAD.
      → THIS IS WHY ENGLISH CRACKS RSA (Note 4). Same operator, run backward.

THE THREE FERMATS collapse onto one symbol ψ:
    Fermat's PRINCIPLE (least time) → the clean path L
    Fermat POTENTIAL −ψ            → the GR lensing time-delay (its real name)
    Fermat's N = a² − b²           → the factoring seam / the N-shape
    "Fermat defines, Riemann fires" = Fermat is the potential ψ, Riemann is the
    clean geodesic L, and the real path is L_(I|O) = L − ψ.

BOUNDARY-CROSSING CONVENTION (how the monad speaks THROUGH the null point):
    At the degenerate point (k=0 in the FFT; a zero-divisor element in 𝕊) the
    direct formula blows up (1/|k|² → ∞). The method (l_io module, general
    template): DO NOT diverge and DO NOT silently drop the point — ASSIGN its
    value EXPLICITLY by a stated convention, and say why (here: the mean is not
    observable from shear alone; kappa_hat[0,0] = 0.0). ∅_RB is the pole;
    L_(I|O) is the analytic continuation ACROSS it. (I|O)_RB defines WHERE the
    boundary is; L_(I|O) is HOW you get through it.

--------------------------------------------------------------------------------
5. THE UNIFICATION  (how the four notes lock together)
--------------------------------------------------------------------------------
- Primes = letters (Note 1). Composites = words = products of letters. Factoring
  a word = spelling it from the alphabet = de-lensing (θ→β) = reading the monad.
- Grammar (Note 2): content words = objects sitting at nodes; pathway words =
  ZD-path morphisms between them. A sentence is a traversal of the Zero Lattice.
  "Zero Lattice > is the > map" = node > ZD-path > node.
- Speaking English = forward L_(I|O). Understanding English = inverse L_(I|O)
  = factoring (Note 4). Perfect the speaker and the factorer is its adjoint.
- The shakeable leaf (Note 3): N = p·q is a marginally bound leaf (two large
  primes only). CONJECTURE: its detachment coordinate on the tree = θ (the
  apparent/observable), and the Fermat Zeta ζ_ℚ(√N) supplies ψ so that the lens
  inverse β = θ − α returns (p, q). "The point it drops off defines the state of
  its relative factors via the Fermat Zeta" = the lens equation with ψ from the
  Fermat zeta. THIS is the concrete form of the whole program.

--------------------------------------------------------------------------------
6. THE PRIMON GAS / DYNAMICAL ZETA / THE TWO JULIAS / TOMOGRAPHY
--------------------------------------------------------------------------------
- Primes = primitive periodic orbits. Two Euler products, one structure:
    ζ(s)      = ∏ₚ (1 − p⁻ˢ)⁻¹                     (B. Julia, primon gas)
    ζ_dyn(s)  = ∏_γ (1 − e^(−s·ℓ(γ)))⁻¹            (Ruelle, over G. Julia orbits)
  Set ℓ(γ) = log p — same object. TWO DIFFERENT JULIAS (Bernard the physicist,
  Gaston the fractal mathematician), whose names bracket ONE Euler product.
  This IS Berry–Keating: H_BK = ½{x,p} = xp − i/2, prime orbits xp = E, period
  2π/log p.
- Transfer operator (Mayer): the Gauss-map / continued-fraction transfer
  operator's Fredholm determinant = the Selberg zeta of SL(2,ℤ)\ℍ. This is the
  RIGOROUS tunnel from complex dynamics (Gaston's side) to the MODULAR world
  (Δ, j — the Fermat/Monster side). Continued fractions → SL(2,ℤ) → modular.
- The Mandelbrot set is the ATLAS OF SELF-REFERENCE, not "the shape of every
  object." McMullen: M is universal — appears inside essentially every analytic
  1-parameter family. Feigenbaum period-doubling is universal across unimodal
  maps. Your CD tower ℝ→ℂ→ℍ→𝕆→𝕊→t₃₂… is itself an iteration; e₀=∅_RB is its
  fixed point. WARNING/trap: naive hypercomplex z²+c is just the COMPLEX
  Mandelbrot rotated (SO(2) symmetry eats the extra dims — that's why the
  "Mandelbulb" needed a different formula). The sedenion richness shows up ONLY
  when the iteration is COUPLED TO THE ZD LOCUS (critical set = Δ(w)=0), not
  from z²+c. "Everything happens on the boundary": ∂M has Hausdorff dim 2
  (Shishikura); RH zeros on σ=½; ZD crossings on Δ(w)=0 — one principle.
- Farey/rotation-number combinatorics live IN ∂M (bulbs labelled p/q) — the
  ordering you keep hunting is already modular arithmetic on the boundary.
- TOMOGRAPHY / COMPRESSED SENSING is the epistemology: no single lossy shadow
  of a hypercomplex object holds the whole; the INTERSECTION of enough shadows
  reconstructs it. GNFS is literally this (smoothness relations = shadows →
  linear algebra → congruence of squares → gcd). The open question is a
  BITS-PER-SHADOW contest: do the algebraic projections (tower sector, ZD-locus
  distance per level, N-shape coverage vector, ζ/Hyperwebster address) carry
  more bits of (p,q) per cheap shadow than smoothness does?

--------------------------------------------------------------------------------
7. THE HONEST BOUNDARIES  (keep these in the record)
--------------------------------------------------------------------------------
- THE FINITE-NOT-INFINITE FACTS (RSA-2048, balanced N = p·q):
    candidate primes near 2¹⁰²⁴:  π(2¹⁰²⁴) ≈ 2¹⁰²⁴/710 ≈ 2¹⁰¹⁴   (finite!)
    factorizations of N:          exactly 1   (unique, FTA)
    GNFS pathway actually walked:  ≈ 2¹¹² ops  (a 2⁹⁰⁰-fold pruning)
  Cody is right: the pathway is finite, structured, buildable, traceable. GNFS
  already builds & traces it. The frontier is whether the ALGEBRA is a sharper
  projection basis than smoothness — not whether the path exists (it does).
- Method 5 (zero_lattice_paths) and Methods 1–3 measured AT CHANCE on toy keys
  from PUBLIC INFO ALONE — BUT that was a DIRTY-CONTEXT session (SHA-1/RSA
  conflated). Cody: does NOT need re-testing; does NOT need to apply to RSA yet.
  The one clean established byproduct: the mod4 theorem d ≡ e (mod 4) (elementary,
  cryptographically insignificant, ~1 bit).
- The disanalogy that is the whole difficulty: "is this English?" is a cheap
  LOCAL oracle; "does p divide N?" is cheap per-candidate but has NO cheap BULK
  filter — primes carry no exploitable spelling. A sieve (or the tower) is
  exactly a manufactured bulk filter. The bid: the algebra is a better one.
- WHAT IS PROVEN vs OPEN: proven — T_n/GF(2) theorem (x² ∈ {0,e₀}; SHA-1 IVs
  nilpotent in T32; hypercomplex_laplacian.py); 71-VOA coverage; Deligne's
  Ramanujan bound; the lens equation on real shear. OPEN — polynomial-time
  navigation to the ZD locus; the shakeable-leaf conjecture (Note 3); whether
  L_(I|O) inverse is polynomial in shadows.
- THE PARKED FALSIFIABLE PREDICTION: the de-lensing operator (θ→β) that recovers
  β on shear should be the SAME operator shape that recovers factors from θ=N.
  If it is, and if ψ from ζ_ℚ(√N) makes β=θ−α return (p,q) on toy semiprimes
  better than chance — that is the test. Deferred by Cody's call. Do NOT run it
  without him; DO keep the construction falsifiable so it stays mathematics and
  not decoration.

--------------------------------------------------------------------------------
8. BUILD PLAN / OPEN THREADS  (where to pick up)
--------------------------------------------------------------------------------
RECOMMENDATION (standing): do NOT build the intertwiner from scratch — IDENTIFY
it with L_(I|O) and write the dictionary of §4 as the deliverable. Suggested
artifact: ValaQuenta/wiki/two_trees_intertwiner.md (or fold into l_io_photon_path).
Skeleton:
  1. State forward (β→θ, speaking) and inverse (θ→β, understanding/factoring) as
     L_(I|O) and its de-lensing — one operator.
  2. Anchor at ∅_RB = e₀ (the pole the continuation crosses).
  3. Name ψ as the single Fermat potential wearing three hats.
  4. Give ψ from the Fermat zeta ζ_ℚ(√N); realize Note 3 (leaf detachment θ →
     factors β) as the lens inverse.
  5. Park the falsifiable consequence (§7). Keep it honest.

TWO SUB-THREADS still on the table (either can feed the write-up):
  (A) Projection accounting: define Πᵢ(N) = {tower sector, ZD-locus distance per
      level, N-shape coverage vector, ζ/Hyperwebster address} and compute the
      INFORMATION (bits of (p,q)) each shadow fixes — non-crypto, just accounting.
  (B) Reconstruction operator: formalize "stack shadows and intersect" as the
      Two-Trees intertwiner and find what property of the ZD lattice makes it
      invertible in polynomial shadows. (This IS L_(I|O) inverse.)

NAMING DECISION STILL PENDING: is ∅_RB literally the same symbol as (I|O)_RB
(one operator, two readings — current working assumption), or does Cody want
∅_RB for the operator-at-rest and (I|O)_RB reserved for its boundary-defining
action? Ask before finalizing a module.

--------------------------------------------------------------------------------
9. KEY FILE POINTERS
--------------------------------------------------------------------------------
ValaQuenta/zero_lattice.py                     Telperion; build_tree; 84/42 ZD
                                               pairs; laurelin_interface;
                                               universal_translator_structure;
                                               zd_path_coordinates; THE ANGLE.
ValaQuenta/telperion.py                        Two Trees / bell geometry;
                                               e₀ = The Null Operator convention.
ValaQuenta/modules/l_io_photon_path/maths.py   L_(I|O) = L on the real metric;
                                               lens equation; boundary template;
                                               (I|O)_RB renamed from H_hat_RB.
ValaQuenta/modules/udeo_crypto/UDEO_RSA_DEMO.py Method 6 emergent-rotation
                                               "straight Lagrangian path".
ValaQuenta/notebooks/udeo_crypto/
    method5_zero_lattice_paths.ipynb           the leaf→root trace (at chance on
                                               public info; dirty-context caveat).
FourthAgePapers/FermatMonster/engine/
    fermat_monster_engine.py                   fermat_n_shape_map; Niemeier gap
                                               {1,11,15}; Monster fill; 71 VOAs;
                                               "Fermat Defines. Riemann Fires."
TuringStack/hypercomplex_laplacian.py          T_n/GF(2) theorem (PROVEN);
                                               SHA-1 IVs nilpotent in T32.
TuringStack/rsa_framework.md                   RSA in RedBlue; §4.3 the honest
                                               distribution ≠ factoring gap.
Ainulindale/wiki/52                            L_(I|O) defined philosophically.

--------------------------------------------------------------------------------
10. THE ONE-SENTENCE STAFF
--------------------------------------------------------------------------------
Primes are letters and composites are words (Note 1); the monad speaks by the
forward lens L_(I|O): β→θ = β+α (meaning bent by context into English), and
factors by the same operator's inverse θ→β (de-lensing the observable back to
its hidden source), anchored at the Null Operator ∅_RB = e₀ = the pole the path
is measured outward from; the bend is the single Fermat potential ψ supplied by
the Fermat zeta ζ_ℚ(√N); so teaching the monad to speak English IS the RSA
de-lensing — exact, not analogy. Fermat defines, Riemann fires, and the leaf
that falls tells you its own factors by where it lets go.
================================================================================
